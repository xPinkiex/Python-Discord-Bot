Bong Bot Todo
===========

## 30.05.2026
- [x] Voice channel status (now playing)
- [x] Song queue
- [x] Timezone storage
- [x] URL summarization
- [x] Bot stats command
- [x] Streaming + message splitting
- [x] Loop/shuffle mutual exclusion
- [x] Conversation summarization
- [x] Debounce song stats saves (60s batch)
- [x] Code review fixes (double-invocation, dead code, asyncio deprecation)
- [x] Retry logic on LLM timeouts (2 retries, 2s/4s backoff)
- [x] Shutdown clears VC status

## 31.05.2026
- [x] Fix duplicate/triple response bug — message deduplication in on_message
- [x] Remove ?list= from YouTube URLs to prevent playlist downloads
- [x] Admin commands — @memories, @forget_user, @delete_memory
- [x] Refactor manage_memory.py into importable functions (CLI unchanged)
- [x] Reminders at specific day/time — timezone-aware with parse_absolute_time
- [x] User cooldown (60s for regular users, admin/authorized bypass)
- [x] Unit tests for cooldown, deduplication, and reminder parsing
- [x] Voice commands module — faster-whisper STT, wake word "hey bong", authorized+ only, voice_commands.py with BongVoiceSink

## 01.06.2026
- [x] Train custom "hey bong" wake word model (TTS + real augmented data, ONNX)
- [x] Integrate hey_bong.onnx into voice_commands.py with fallback to hey_jarvis
- [x] Rename GitHub repo to Bong-Discord-Bot
- [x] Add .pytest_cache, whisper_models/, wakeword_training/ to .gitignore
- [x] Track hey_bong.onnx in git (ignore other wakeword_models)
- [x] Voice commands polish — respond in VC text channel, skip classifier on wake word, test different whisper model sizes

## Backlog
- [ ] Per user token counting with a leaderboard like music. Maybe make a leaderboards.json where all the leaderboards are kept.
- [ ] Code overhaul — move source to src/, break up bong_tools.py into separate modules (music, memory, reminders, web, voice, admin, etc.)
- [ ] Per-tool call limit — allow only X runs of a specific tool in a single tool loop chain
- [ ] Loop/shuffle queue mode — loop the entire queue if >1 song; shuffle the queue if >1 song instead of pulling from the whole folder
- [ ] Rule-based tool provisioning — only bind relevant tools based on context (voice/music if caller_in_voice, vision if image attachments, text files if text attachments, core tools always on)
- [ ] Bong-controlled shuffle — LLM picks next song based on conversation context
- [ ] Playlist support — save/load named playlists from JSON
- [ ] Rate limiting on tools — prevent spamming web_search, download_music, etc.
